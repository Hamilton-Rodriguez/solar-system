apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  name: solar-system
  namespace: ${NAMESPACE}
spec:
    rules:
    - host: solar-system-${NAMESPACE}.${INGRESS_IP}.nip.io
      http:
        paths:
        - backend:
            service:
              name: solar-system
              port:
                number: 3000
          path: /
          pathType: Prefix
    tls:
    - hosts:
      - solar-system-${NAMESPACE}.${INGRESS_IP}.nip.io
      secretName: ingress-local-tls



# AWS working version with single environment /

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: solar-system
  namespace: ${NAMESPACE}
  annotations:
    # Disables automatic HTTP->HTTPS redirect while testing without SSL certs
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  # Updated annotation to modern K8s spec
  ingressClassName: nginx
  rules:
    # Removing the explicit 'host' rule allows NGINX to catch all incoming traffic on the NLB
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: solar-system
                port:
                  number: 3000



# /dev path version - But not working in coexistance with /prod

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: solar-system-dev
  namespace: ${NAMESPACE}
  annotations:
    # Disables automatic HTTP->HTTPS redirect while testing without SSL certs
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  # Updated annotation to modern K8s spec
  ingressClassName: nginx
  rules:
    # Removing the explicit 'host' rule allows NGINX to catch all incoming traffic on the NLB
    - http:
        paths:
          - path: /dev(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: solar-system
                port:
                  number: 3000

little change - test solution
